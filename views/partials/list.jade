md-list
    md-list-item(ng-repeat="product in vm.item.items")
        div(class="md-list-item-text")
            h3 {{product.title}}
            p {{product.price}}
            md-divider(ng-if="!$last")
